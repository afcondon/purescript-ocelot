{
  "name": "purescript-ocelot",
  "version": "0.30.0",
  "private": true,
  "scripts": {
    "build-all": "make build",
    "clean": "make clean",
    "build-ui": "make build-ui",
    "build-css": "make build-css",
    "debug": "yarn parcel dist/parcel.html --out-dir .build/parcel --no-cache"
  },
  "devDependencies": {
    "parcel": "2.0.0-beta.2",
    "purescript": "0.14.2",
    "spago": "^0.20.3"
  },
  "dependencies": {
    "big-integer": "^1.6.48",
    "request": "^2.88.2",
    "yarn": "^1.22.10"
  }
}
